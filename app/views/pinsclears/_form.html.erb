<%= form_for(@pinsclear) do |f| %>
  <% if @pinsclear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pinsclear.errors.count, "error") %> prohibited this pinsclear from being saved:</h2>

      <ul>
      <% @pinsclear.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
