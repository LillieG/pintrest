<h1>New Pin</h1>
  <tr>

    <div class="form-group">
  <label for="usr">Title:</label>
  <input type="text" class="form-control" id="usr">
</div></tr>
      <th colspan="100%"></th>--)
  
   <tr>
<%= render 'form' %>

<%= link_to 'Back', pins_path %>

.table-striped

setting up Github

from workspace
git config --global user.name "lillieg"
git config --global user.email llglasgo@gmail.com
git config --global push.default matching
git config --global alias.co checkout
git init
git add .git
git commit -am "any comments"
git push

cat ~/.ssh/id_rsa.pub [displays the SSH Key to be added to 
  GitHub respository - go to github create a respository and enter SSH key to link to C9.io ]
  
git remote add origin
git@github.com[lillieg/respository_name].git
git push -u origin master
rake db:migrate


heroku run rake db:migrate --app myapp > rake.log
cat rake.log

(set up)

heroku --version
heroku login
heroku keys:add
heroku create
heroku rename (to what ever app name you want)

her
cd my-project/
git init
heroku git:remote -a lillieapp
git add .
git commit -am "make it be
git push heroku master


$ heroku apps:rename newname

Delete app from local and heroku:
heroku apps:destroy –app appname

That’s it. …!!

(if you have to remove  heroku: type the following)
$ wget -O- https://toolbelt.heroku.com/install-ubuntu.sh 